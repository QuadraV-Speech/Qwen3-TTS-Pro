# Contributing

欢迎提交问题、文档、测试、WebUI 和推理性能改进。

提交改动前请：

1. 说明使用的 GPU、驱动、Python、vLLM 和 vLLM-Omni 版本。
2. 对推理补丁执行 `./scripts/validate_repo.sh`。
3. 性能改动同时报告成功率、TTFP、RTF、音频秒吞吐和请求吞吐。
4. 将实验能力默认关闭，除非重复 A/B 数据证明其收益且没有明显尾延迟回退。
5. 不提交模型权重、音频隐私数据、缓存、运行日志、PID 或本机绝对路径。

补丁目标固定为 `vLLM-Omni v0.26.0`。若要升级上游版本，请单独提交 rebase，并附带补丁可应用性与核心流式测试结果。
